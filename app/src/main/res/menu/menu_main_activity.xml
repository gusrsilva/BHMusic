<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.blockhead.bhmusic.activities.MainActivity">
    <!-- Search, should appear as action button -->
    <item
        android:id="@+id/action_now_playing"
        android:icon="@drawable/ic_action_now_playing"
        android:showAsAction="ifRoom"
        android:title="@string/action_now_playing" />

    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:showAsAction="never"
        android:title="@string/action_settings" />

    <item
        android:id="@+id/action_exit"
        android:orderInCategory="1"
        android:showAsAction="never"
        android:title="@string/action_exit" />
    <item
        android:id="@+id/action_shuffle_all"
        android:orderInCategory="2"
        android:showAsAction="never"
        android:title="@string/action_shuffle_all" />
</menu>
